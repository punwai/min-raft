/*
 * For RAFT -- we are aiming to create a distributed KV store.
 * And in this sense, we are trying to replicate a log of get-set commands.
 * We will then attempt to crash the node and see if the protocol still works.
 */
